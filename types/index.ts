export type UserInfo = {
    name: string;
    email: string;
    image: string;
}

export type Post = {
    id: string;
    name: string;
    image?: string;
    text: string;
    timestamp: string;
    avatar: string;
}